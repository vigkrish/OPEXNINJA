services:
  - type: web
    name: opexninja-api
    runtime: node
    plan: free
    buildCommand: npm install && npm -w @opexninja/api run db:generate && npm -w @opexninja/api run build
    startCommand: npm -w @opexninja/api run start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        # GitHub Pages project-site origin. Add custom domains here when they are configured.
        value: https://vigkrish.github.io
      - key: DATABASE_URL
        fromDatabase:
          name: opexninja-db
          property: connectionString

databases:
  - name: opexninja-db
    plan: free
